{
  "name": "yoomoney",
  "version": "1.0.0",
  "scripts": {
    "start": "node -e 'const fs = require(\"fs\"); fs.readFile(\"nixpacks.toml\", \"utf8\", (err, data) => { if (err) throw err; console.log(data); });'"
  },
  "dependencies": {}
}
